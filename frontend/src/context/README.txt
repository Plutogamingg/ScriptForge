Purpose: Manage and provide application-wide state using React's Context API.
Examples:
- `UserContext.js`: Context for user authentication state.
- `ThemeContext.js`: Context for managing UI themes.
Each context file typically contains a context provider and the related consumer hooks.
