import template from './containers.html';
import controller from './containers.controller';

function routeConfig($stateProvider) {
	'ngInject';

	$stateProvider
		.state('app.component.containers', {
			url: '/containers',
			template,
			controller,
			controllerAs: 'vm'
		});
}

export default routeConfig;